{
  "name": "cronpilot-relay",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@9.15.5",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "test": "node --test src/index.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.33.0"
  }
}
