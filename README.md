# CronPilot

Schedule AI tasks like cron jobs.

CronPilot is an open-source scheduler for recurring AI tasks. Define a prompt, a cron expression, and an OpenAI-compatible model endpoint; CronPilot runs the task on schedule and records the result.

> Early development. The first milestone is a small, self-hostable core: schedule -> LLM -> result.
